#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int t;
    scanf("%d",&t);
    while(t--)
    {
        int pos;
        scanf("%d",&pos);
        int n;
        scanf("%d",&n);
        int arr[n];
        for(int i=0;i<n;i++)
        {
            scanf("%d",&arr[i]);
        }
        for(int i=pos;i<n;i++)
            printf("%d ",arr[i]);
        for(int i=0;i<pos;i++)
            printf("%d ",arr[i]);
        printf("\n");
            
    }
    return 0;
}
